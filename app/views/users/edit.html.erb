<% provide(:title, "Edit User") %>

<h1>Update your profile</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form', btn_val: "Save Changes" %>

    Wanna leave?
    <%= link_to fa_icon('trash') + " Delete your account", @user, method: :delete, data: { confirm: "Are you sure?"}, class: 'btn btn-danger' %>
  </div>
</div>

